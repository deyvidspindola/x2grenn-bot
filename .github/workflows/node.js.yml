# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16.x'

      - name: Install Dependencies
        run: npm install

      - name: Make envfile
        uses: SpicyPizza/create-envfile@v1
        with:
          envkey_BET_URL: ${{ secrets.BET_URL }}
          envkey_BET_API_URL: ${{ secrets.BET_API_URL }}
          envkey_BET_API_KEY: ${{ secrets.BET_API_KEY }}
          envkey_MONGODB_DRIVER: ${{ secrets.MONGODB_DRIVER }}
          envkey_MONGODB_URI: ${{ secrets.MONGODB_URI }}
          envkey_MONGODB_DATABASE: ${{ secrets.MONGODB_DATABASE }}
          envkey_MONGODB_USERNAME: ${{ secrets.MONGODB_USERNAME }}
          envkey_MONGODB_PASSWORD: ${{ secrets.MONGODB_PASSWORD }}
          envkey_TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          envkey_TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          fail_on_empty: false
          sort_keys: false

      - name: Build
        run: npm run clean && tsc

      - name: Commit Build to Branch
        run: |
          git config --global user.email "deyvidrafhael@gmail.com"
          git config --global user.name "Deyvid Spindola"
          git checkout -b build
          git add -f dist
          git commit -m "Add build artifacts"
          git push -f origin build
